if SHIELD_JSON_LOWPRO_LFT

config ZMK_KEYBOARD_NAME
    default "json_lowpro lft"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
    default y

endif

if SHIELD_JSON_LOWPRO_RGHT

config ZMK_KEYBOARD_NAME
    default "json_lowpro rght"

endif

if SHIELD_JSON_LOWPRO_LFT || SHIELD_JSON_LOWPRO_RGHT

config ZMK_SPLIT
    default y

endif
